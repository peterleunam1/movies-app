export interface AccordionInterface {
    title: string,
    height?: string,
    wChildren?: string,
    withButton: boolean,
    opened?: boolean,
    mt?: string,
    w?:string,
    children: React.ReactNode,
  }